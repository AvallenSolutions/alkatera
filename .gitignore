# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/.next-*/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# temporary design exports / imports
*-marketing-export/
*-import/
# Exception: bulk-import is part of the codebase
!lib/bulk-import/
!app/api/bulk-import/
!components/bulk-import/
Alkatera Marketing/
STRIPE_PRODUCTION_KEYS.txt
STRIPE_TEST_KEYS.txt

# Local Netlify folder
.netlify

# Business documents and exports — confidential, never commit.
# (Contracts, finance models, briefings etc. live in the repo root while
# being worked on; a `git add .` must not sweep them into history.)
*.docx
*.xlsx
*.pptx
*.pdf
*.doc
*.xls
*.ppt
.~lock.*#
Knowledge Bank - */

# Claude Code worktrees (stale checkouts; also excluded from vitest)
.claude/worktrees/
scripts/eval-out/
.env*

# Local LCA database downloads (ecoinvent .zolca libraries, ~9 GB).
# Never belongs in git; sat untracked and unignored until 24 Jul 2026.
OpenLCA/
